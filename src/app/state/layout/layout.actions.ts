import { createAction } from '@ngrx/store';

export const activeDarkMode = createAction('[Layout Component] ActiveDarkMode');
export const activeLightMode = createAction('[Layout Component] ActiveLightMode');